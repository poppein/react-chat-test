import React from 'react';
import PropTypes from 'prop-types';
import Chat from '../components/Chat';

const Root = () => {
    return (
        <div>
            <Chat />
        </div>
    );
};

export default Root;